import React from 'react';
import { Typography, Button } from '@mui/material'; // Import these components

const ThemeSection = () => {
  return (
    <div>
      <h2>Theme Section</h2>
      <p>Change the theme as needed.</p>
    </div>
  );
};

export default ThemeSection;
